spring.application.name=site-institucional

# PostgreSQL Configuration
spring.datasource.url=jdbc:postgresql://ep-falling-salad-ac6oxhaj-pooler.sa-east-1.aws.neon.tech/neondb?user=neondb_owner&password=npg_b5Pefp0tGZsq&sslmode=require
spring.datasource.username=${neondb_owner}
spring.datasource.password=${npg_b5Pefp0tGZsq}
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA/Hibernate properties
spring.jpa.hibernate.ddl-auto=update
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

spring.jpa.show-sql=true

#Lembrar de criar uma conta e pegar o Secret

# Configurações de Email
spring.mail.host=smtp.example.com
spring.mail.port=587
spring.mail.username=${MAIL_USERNAME:}
spring.mail.password=${MAIL_PASSWORD:}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

# Configurações do reCAPTCHA
application.recaptcha.secret=${RECAPTCHA_SECRET:6Lxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx}
application.recaptcha.threshold=0.5

# Configurações de Rate Limiting
application.ratelimiting.enabled=true
application.ratelimiting.limit=10
application.ratelimiting.duration=60

# Configurações de JWT (atualizando para incluir valores das propriedades faltantes)
application.security.jwt.secret-key=${JWT_SECRET:404E635266556A586E3272357538782F413F4428472B4B6250645367566B5970}
application.security.jwt.expiration=86400000
application.security.jwt.refresh-token.expiration=604800000

# Configurações de Monitoramento de Backup
application.backup.monitor.enabled=true
application.backup.monitor.tolerance-hours=25

# Cache Configuration
spring.cache.type=caffeine
spring.cache.caffeine.spec=maximumSize=500,expireAfterWrite=10m

# File Upload Configuration (para fotos de depoimentos)
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB

