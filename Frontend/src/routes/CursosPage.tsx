import React from 'react';

const CursosPage: React.FC = () => {
	return (
		<div style={{ padding: '2rem' }}>
			<h1>Todos os Cursos</h1>
			<p>Aqui será exibida a lista completa de cursos.</p>
		</div>
	);
};

export default CursosPage;
